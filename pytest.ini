# pytest配置文件

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 覆盖率
addopts = --strict-markers --tb=short

# 标记
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
